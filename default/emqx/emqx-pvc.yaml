apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emqx
  namespace: default
spec:
  resources:
    requests:
      storage: 100Mo
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
