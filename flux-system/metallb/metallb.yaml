---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: kube-system
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://charts.bitnami.com/bitnami
      chart: metallb
      version: 2.4.0
      sourceRef:
        kind: HelmRepository
        name: bitnami-charts
        namespace: flux-system
      interval: 5m
  values:
    configInline:
      address-pools:
      - name: default
        protocol: layer2
        addresses:
        - 10.0.63.70-10.0.63.79
      # peers:
      # - peer-address: 10.0.7.1
      #   peer-asn: 64512
      #   my-asn: 64512
      # address-pools:
      # - name: bgp
      #   protocol: bgp
      #   addresses:
      #   -  10.0.6.0/24
      #   avoid-buggy-ips: true
    # prometheusRule:
    #   enabled: true
    # prometheus:
    #   enabled: true
    #   serviceMonitor:
    #     enabled: true
    #   prometheusRule:
    #     enabled: true
