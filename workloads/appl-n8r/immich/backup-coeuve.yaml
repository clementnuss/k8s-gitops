---
# Independent Kopia backup of the immich library PVC to syno-coeuve over
# WebDAV. This is a SECOND backup, independent of the S3 backup
# (backup-s3.yaml). If one repo is corrupted or compromised, the other is
# unaffected. Both repos use the same path + password (shared via
# immich-kopia); only the endpoint creds differ.
#
# Lives in appl-n8r (same namespace as immich) so it can mount the
# immich-library PVC directly.
#
# Secrets:
#   immich-kopia        - KOPIA_REPO_PATH + KOPIA_PASSWORD (shared with S3 job)
#   webdav-syno-coeuve  - KOPIA_WEBDAV_URL/USER/PASS (shared endpoint)
apiVersion: batch/v1
kind: CronJob
metadata:
  name: immich-backup-coeuve
  namespace: appl-n8r
spec:
  schedule: "30 3 * * *"
  timeZone: Europe/Zurich
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: kopia
              image: kopia/kopia:0.23.1
              command:
                - /bin/sh
                - -c
                - |
                  set -eu

                  DST_URL="${KOPIA_WEBDAV_URL%/}/${KOPIA_REPO_PATH}"

                  echo "=== Connecting to (or creating) destination repository: $DST_URL ==="
                  kopia repository connect webdav \
                    --url="$DST_URL" \
                    --webdav-username="$KOPIA_WEBDAV_USER" \
                    --webdav-password="$KOPIA_WEBDAV_PASS" \
                    --override-username=immich \
                    --override-hostname=planchettes63 || \
                  kopia repository create webdav \
                    --url="$DST_URL" \
                    --webdav-username="$KOPIA_WEBDAV_USER" \
                    --webdav-password="$KOPIA_WEBDAV_PASS"

                  echo "=== Repository status ==="
                  kopia repository status

                  # Ensure this identity is the maintenance owner — the repo
                  # was originally created by root@<pod-name> before
                  # --override-username/--override-hostname were set, and
                  # only the maintenance owner can run maintenance.
                  kopia maintenance set --owner=immich@planchettes63

                  kopia policy set --global \
                    --keep-latest=7 \
                    --keep-daily=7 \
                    --keep-weekly=4 \
                    --keep-monthly=6 \
                    --keep-annual=1

                  echo "=== Creating snapshot of /data ==="
                  kopia snapshot create /data/backups /data/library /data/upload

                  echo "=== Snapshots ==="
                  kopia snapshot list --all

                  echo "=== Running quick maintenance ==="
                  kopia maintenance run

                  kopia repository disconnect
                  echo "=== Done ==="
              envFrom:
                # Per-repo: KOPIA_REPO_PATH + KOPIA_PASSWORD.
                - secretRef:
                    name: immich-kopia
              env:
                # Persist the repo connection config on the cache PVC so
                # reconnects are instant (no index re-download each run).
                - name: KOPIA_CONFIG_PATH
                  value: /root/.cache/kopia/repository.config
                # Endpoint creds (shared webdav-syno-coeuve).
                - name: KOPIA_WEBDAV_URL
                  valueFrom:
                    secretKeyRef:
                      name: webdav-syno-coeuve
                      key: KOPIA_WEBDAV_URL
                - name: KOPIA_WEBDAV_USER
                  valueFrom:
                    secretKeyRef:
                      name: webdav-syno-coeuve
                      key: KOPIA_WEBDAV_USER
                - name: KOPIA_WEBDAV_PASS
                  valueFrom:
                    secretKeyRef:
                      name: webdav-syno-coeuve
                      key: KOPIA_WEBDAV_PASS
              volumeMounts:
                - name: data
                  mountPath: /data
                  readOnly: true
                - name: kopia-cache
                  mountPath: /root/.cache/kopia
              resources:
                requests:
                  cpu: 100m
                  memory: 256Mi
                limits:
                  memory: 2Gi
          volumes:
            # Cached NFS mount (actimeo=30, ro) — see immich-library-backup-pvc.yaml.
            - name: data
              persistentVolumeClaim:
                claimName: immich-library-backup
            - name: kopia-cache
              persistentVolumeClaim:
                claimName: immich-kopia-cache-coeuve
---
# Weekly deep verification of the immich coeuve backup repository.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: immich-verify-coeuve
  namespace: appl-n8r
spec:
  schedule: "0 4 * * 0"
  timeZone: Europe/Zurich
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: kopia
              image: kopia/kopia:0.23.1
              command:
                - /bin/sh
                - -c
                - |
                  set -eu

                  DST_URL="${KOPIA_WEBDAV_URL%/}/${KOPIA_REPO_PATH}"

                  echo "=== Connecting to destination for verification: $DST_URL ==="
                  kopia repository connect webdav \
                    --url="$DST_URL" \
                    --webdav-username="$KOPIA_WEBDAV_USER" \
                    --webdav-password="$KOPIA_WEBDAV_PASS"

                  echo "=== Verifying all snapshots (downloads and hashes every block) ==="
                  kopia snapshot verify --all-snapshots

                  echo "=== Running full maintenance ==="
                  kopia maintenance run --full

                  kopia repository disconnect
                  echo "=== Verification complete ==="
              envFrom:
                # Per-repo: KOPIA_REPO_PATH + KOPIA_PASSWORD.
                - secretRef:
                    name: immich-kopia
              env:
                # Endpoint creds (shared webdav-syno-coeuve).
                - name: KOPIA_WEBDAV_URL
                  valueFrom:
                    secretKeyRef:
                      name: webdav-syno-coeuve
                      key: KOPIA_WEBDAV_URL
                - name: KOPIA_WEBDAV_USER
                  valueFrom:
                    secretKeyRef:
                      name: webdav-syno-coeuve
                      key: KOPIA_WEBDAV_USER
                - name: KOPIA_WEBDAV_PASS
                  valueFrom:
                    secretKeyRef:
                      name: webdav-syno-coeuve
                      key: KOPIA_WEBDAV_PASS
              volumeMounts:
                - name: kopia-cache
                  mountPath: /root/.cache/kopia
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  memory: 1Gi
          volumes:
            - name: kopia-cache
              emptyDir: {}