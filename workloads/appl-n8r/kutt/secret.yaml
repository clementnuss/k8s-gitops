---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kutt-secret
  namespace: appl-n8r
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: kutt-secret
    creationPolicy: Owner
    template:
      type: Opaque
  dataFrom:
  - extract:
      key: k8s/appl-n8r/kutt-secret

