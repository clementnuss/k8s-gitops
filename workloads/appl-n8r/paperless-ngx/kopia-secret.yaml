---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: paperless-kopia-secret
  namespace: appl-n8r
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: paperless-kopia-secret
    creationPolicy: Owner
    template:
      type: Opaque
  dataFrom:
  - extract:
      key: k8s/appl-n8r/paperless-kopia-secret

